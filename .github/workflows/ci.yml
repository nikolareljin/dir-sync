name: CI

on:
  pull_request:
  push:
    branches: [main, master, "release/*"]

jobs:
  ci:
    uses: nikolareljin/ci-helpers/.github/workflows/python.yml@0.1.1
    with:
      python_version: "3.11"
      working_directory: "."
