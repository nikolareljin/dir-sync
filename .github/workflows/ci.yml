name: CI

on:
  pull_request:
  push:
    branches: [main, master, "release/*"]

jobs:
  ci:
    uses: nikolareljin/ci-helpers/.github/workflows/python-scan.yml@0.1.2-rc
    with:
      python_version: "3.11"
      working_directory: "."
      
